## Random functions
    random(min, max)
    noise(xoff)
    noiseDetail(number_octaves, falloff)

## Loop stop
    noLoop()

## Graph
    beginShape()
    // point() or vertex()
    endShape()

## Vector
    createVector(x, y)
    vector.add(vector2)
    p5.Vector.random2D() // unit vector
    vector.mult(scale)
    p5.Vector.add(v1, v2)
    vector.setMag(mag)
